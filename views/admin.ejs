<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin • Veladzic • Ziele</title>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/public/css/styles.css" />
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='0.9em' font-size='90'>🎯</text></svg>">
  </head>
  <body>
    <div class="bg"></div>
    <main class="container">
      <section class="header">
        <h1 class="title">Admin</h1>
        <p class="subtitle">Countdowns verwalten</p>
        <div style="margin-left:auto;display:flex;gap:8px;align-items:center">
          <a class="admin-link" href="/">Startseite</a>
          <% if (authEnabled) { %>
            <a class="admin-link" href="/admin/logout">Abmelden</a>
          <% } %>
        </div>
      </section>

      <section class="panel">
        <% if (authEnabled) { %>
          <p class="note">Admin‑Schutz aktiviert. Du bist angemeldet.</p>
        <% } else { %>
          <p class="note warn">Kein Admin‑Token gesetzt. Jeder kann ändern. Setze <code>ADMIN_TOKEN</code> für Schutz.</p>
        <% } %>
        <h2 class="section-title">Neu hinzufügen</h2>
        <form class="form" method="post" action="/admin/add">
          <div class="row">
            <label>Titel</label>
            <input name="title" placeholder="z. B. Jubiläum" required />
          </div>
          <div class="row">
            <label>Beschreibung</label>
            <input name="description" placeholder="Optionale Details" />
          </div>
          <div class="row two">
            <div>
              <label>Datum</label>
              <input type="date" name="targetDate" required />
            </div>
            <div>
              <label>Uhrzeit</label>
              <input type="time" name="targetTime" value="00:00" required />
            </div>
          </div>
          <div class="row two">
            <div>
              <label>Emoji</label>
              <input name="emoji" placeholder="e.g. 💖" />
            </div>
            <div>
              <label>Akzentfarbe</label>
              <input type="color" name="color" value="#8B5CF6" />
            </div>
          </div>
          <div class="actions">
            <button class="btn">Countdown hinzufügen</button>
          </div>
        </form>
      </section>

      <section class="panel">
        <h2 class="section-title">Alle Countdowns</h2>
        <% if (!countdowns || countdowns.length === 0) { %>
          <div class="empty">Noch keine Countdowns.</div>
        <% } %>
        <div class="list">
          <% (countdowns || []).forEach(c => { %>
            <div class="list-item" style="--accent:<%= c.color || '#8B5CF6' %>">
              <div class="left">
                <div class="emoji small"><%= c.emoji || '⏳' %></div>
                <div class="meta">
                  <div class="name"><%= c.title %></div>
                  <div class="sub"><%= new Date(c.target).toLocaleString() %></div>
                </div>
              </div>
              <div class="right">
                <a class="btn ghost" href="/admin/edit/<%= c.id %>">Bearbeiten</a>
                <form method="post" action="/admin/delete/<%= c.id %>" onsubmit="return confirm('Diesen Countdown löschen?');">
                  <button class="btn danger" type="submit">Löschen</button>
                </form>
              </div>
            </div>
          <% }) %>
        </div>
      </section>
    </main>
  </body>
 </html>
